#include <libng_render/material/Material.hpp>

namespace libng {

Material::Material() {
  LIBNG_LOG("{}\n", __LIBNG_PRETTY_FUNCTION__);
}

Material::~Material() {
  LIBNG_LOG("{}\n", __LIBNG_PRETTY_FUNCTION__);
}

} // namespace libng